<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nuevo Evento</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }
        h1 {
            margin-bottom: 30px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        input[type="number"],
        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 2px solid #ddd;
            border-radius: 5px;
            box-sizing: border-box;
        }
        textarea {
            min-height: 120px;
            resize: vertical;
        }
        .create-btn {
            background-color: #4CAF50;
            color: white;
            padding: 12px 30px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            width: 100%;
        }
        .create-btn:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>Nuevo Evento</h1>
    
    <form action="/new-event" method="POST">
        <div class="form-group">
            <label for="day">Day (1-30)</label>
            <input type="number" id="day" name="day" min="1" max="30" required>
        </div>
        
        <div class="form-group">
            <label for="name">Nombre del Evento</label>
            <input type="text" id="name" name="name" required>
        </div>
        
        <div class="form-group">
            <label for="description">Descripcion</label>
            <textarea id="description" name="description"></textarea>
        </div>
        
        <button type="submit" class="create-btn">Crear</button>
    </form>

    <button onclick="window.location.href='/'" style="margin-bottom: 20px; padding: 8px 16px; cursor: pointer; margin-top: 20px;">Volver</button>
    <footer>VM-2</footer>
</body>
</html>