<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= event.name_event %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }
        h1 {
            margin-bottom: 10px;
        }
        .description {
            margin: 20px 0;
            padding: 15px;
            background-color: #f5f5f5;
            border-radius: 5px;
            line-height: 1.6;
        }
        .buttons {
            display: flex;
            gap: 10px;
            margin-top: 30px;
        }
        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        .back-btn {
            background-color: #666;
            color: white;
        }
        .back-btn:hover {
            background-color: #555;
        }
        .delete-btn {
            background-color: #f44336;
            color: white;
        }
        .delete-btn:hover {
            background-color: #da190b;
        }
    </style>
</head>
<body>
    <h1><%= event.name_event %></h1>
    <p><strong>Dia:</strong> <%= event.day %></p>
    
    <div class="description">
        <%= event.description %>
    </div>
    
    <div class="buttons">
        <button class="btn back-btn" onclick="window.location.href='/'">Volver al calendario</button>
        <button class="btn delete-btn" onclick="window.location.href='/delete/<%= event.id %>'">Borrar evento</button>
    </div>
</body>
</html>